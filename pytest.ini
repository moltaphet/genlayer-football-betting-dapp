[pytest]
# The gltest venv ships a stray `hydra_pytest` plugin that imports antlr4, which
# crashes on Python 3.14 (`typing.io` was removed). Disable it so pytest starts.
addopts = -p no:hydra_pytest
markers =
    slow: real web + LLM calls against the network; deselect with -m "not slow"
